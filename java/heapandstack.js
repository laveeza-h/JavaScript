let name = "Laveeza"; // Primitive (stored in stack)
let age = 21;         // Primitive (stored in stack)

function greet() {
  let message = "Hello!"; // Local variable (in stack)
  console.log(message);
}
greet();
